# Credenciales de la cuenta en demo.traccar.org.
# Solo se leen del lado del servidor (proxy). Nunca con prefijo VITE_,
# porque eso las incrustaria en el bundle del navegador.
TRACCAR_EMAIL=
TRACCAR_PASSWORD=
